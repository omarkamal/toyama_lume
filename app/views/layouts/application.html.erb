<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Toyama Lume" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Toyama Lume">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
    <%# Google Fonts - DM Sans and Poppins %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;700&family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">
    
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              'navy': '#10223A',
              'teal': '#226A73',
              'lime': '#97D700',
              'carbon-light': '#F8F7F7',
              'carbon-heavy': '#323233',
              'cloudy': '#DBDBDB',
            },
            fontFamily: {
              'heading': ['DM Sans', 'sans-serif'],
              'body': ['Poppins', 'sans-serif'],
            }
          }
        }
      }
    </script>
  </head>

  <body class="bg-carbon-light font-body">
    <% if logged_in? %>
      <nav class="bg-white shadow-sm border-b border-cloudy relative">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="flex justify-between h-16">
            <div class="flex items-center">
              <div class="flex-shrink-0 flex items-center">
                <%= link_to root_path, class: "flex items-center gap-3" do %>
                  <%= image_tag "toyama_logo.svg", alt: "Toyama", class: "h-8" %>
                  <span class="text-sm font-heading font-medium text-carbon-heavy/60">Lume</span>
                <% end %>
              </div>
              <div class="hidden sm:ml-8 sm:flex sm:space-x-8">
                <%= link_to "Dashboard", root_path, class: "inline-flex items-center px-1 pt-1 border-b-2 #{request.path == root_path ? 'border-teal text-navy' : 'border-transparent text-carbon-heavy/60 hover:text-navy hover:border-cloudy'} text-sm font-body font-normal transition-colors" %>
                <%= link_to "My Tasks", my_tasks_tasks_path, class: "inline-flex items-center px-1 pt-1 border-b-2 #{request.path == my_tasks_tasks_path ? 'border-teal text-navy' : 'border-transparent text-carbon-heavy/60 hover:text-navy hover:border-cloudy'} text-sm font-body font-normal transition-colors" %>
                <%= link_to "Work Logs", work_logs_path, class: "inline-flex items-center px-1 pt-1 border-b-2 #{request.path == work_logs_path ? 'border-teal text-navy' : 'border-transparent text-carbon-heavy/60 hover:text-navy hover:border-cloudy'} text-sm font-body font-normal transition-colors" %>
                <% if current_user.admin? %>
                  <%= link_to "Admin", admin_root_path, class: "inline-flex items-center px-1 pt-1 border-b-2 #{request.path.start_with?(admin_root_path) ? 'border-teal text-navy' : 'border-transparent text-carbon-heavy/60 hover:text-navy hover:border-cloudy'} text-sm font-body font-normal transition-colors" %>
                <% end %>
              </div>
            </div>
            <div class="flex items-center gap-2">
              <span class="hidden sm:block text-sm font-body font-light text-carbon-heavy/70 mr-4">Hi, <%= current_user.name %>!</span>
              <span class="hidden sm:block">
                <%= button_to "Logout", logout_path, method: :delete, class: "bg-navy hover:bg-navy/90 text-white px-4 py-2 rounded-lg text-sm font-body font-normal transition-all" %>
              </span>

              <!-- Mobile menu button -->
              <button
                type="button"
                class="sm:hidden inline-flex items-center justify-center p-2 rounded-md text-carbon-heavy/60 hover:text-navy hover:bg-carbon-light transition-colors"
                onclick="toggleMobileMenu()"
                aria-expanded="false">
                <span class="sr-only">Open main menu</span>
                <!-- Menu icon -->
                <svg class="mobile-menu-open h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
                </svg>
                <!-- Close icon -->
                <svg class="mobile-menu-close h-6 w-6 hidden" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                </svg>
              </button>
            </div>
          </div>
        </div>

        <!-- Mobile menu overlay -->
        <div id="mobile-menu" class="hidden sm:hidden fixed inset-0 z-50">
          <!-- Backdrop -->
          <div class="fixed inset-0 bg-black/50" onclick="toggleMobileMenu()"></div>

          <!-- Menu panel -->
          <div class="fixed right-0 top-0 h-full w-80 max-w-full bg-white shadow-xl transform transition-transform duration-300 ease-in-out">
            <div class="flex flex-col h-full">
              <!-- Header -->
              <div class="bg-navy text-white px-6 py-4">
                <div class="flex items-center justify-between">
                  <div>
                    <p class="text-lime text-xs uppercase tracking-wide mb-1">Welcome</p>
                    <h2 class="text-lg font-heading font-medium"><%= current_user.name %></h2>
                    <p class="text-white/60 text-sm font-body font-light"><%= current_user.email %></p>
                  </div>
                  <button
                    onclick="toggleMobileMenu()"
                    class="p-2 rounded-md text-white/60 hover:text-white hover:bg-white/10 transition-colors">
                    <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                    </svg>
                  </button>
                </div>
              </div>

              <!-- Navigation -->
              <nav class="flex-1 px-4 py-6 space-y-2 overflow-y-auto">
                <%= link_to root_path, class: "flex items-center gap-3 px-4 py-3 rounded-lg text-gray-700 hover:bg-carbon-light hover:text-navy transition-colors #{'bg-teal/10 text-teal' if request.path == root_path}" do %>
                  <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" />
                  </svg>
                  <div>
                    <div class="font-body font-normal">Dashboard</div>
                    <div class="text-xs text-carbon-heavy/60">Overview and quick actions</div>
                  </div>
                <% end %>

                <%= link_to my_tasks_tasks_path, class: "flex items-center gap-3 px-4 py-3 rounded-lg text-gray-700 hover:bg-carbon-light hover:text-navy transition-colors #{'bg-teal/10 text-teal' if request.path == my_tasks_tasks_path}" do %>
                  <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2" />
                  </svg>
                  <div>
                    <div class="font-body font-normal">My Tasks</div>
                    <div class="text-xs text-carbon-heavy/60">Manage your tasks</div>
                  </div>
                <% end %>

                <%= link_to work_logs_path, class: "flex items-center gap-3 px-4 py-3 rounded-lg text-gray-700 hover:bg-carbon-light hover:text-navy transition-colors #{'bg-teal/10 text-teal' if request.path == work_logs_path}" do %>
                  <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
                  </svg>
                  <div>
                    <div class="font-body font-normal">Work Logs</div>
                    <div class="text-xs text-carbon-heavy/60">View your work history</div>
                  </div>
                <% end %>

                <!-- Future features -->
                <div class="flex items-center gap-3 px-4 py-3 rounded-lg text-gray-400 cursor-not-allowed">
                  <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
                  </svg>
                  <div>
                    <div class="font-body font-normal">Leave Management</div>
                    <div class="text-xs text-carbon-heavy/60">Coming soon</div>
                  </div>
                </div>

                <%= link_to employee_handbook_path, class: "flex items-center gap-3 px-4 py-3 rounded-lg text-gray-700 hover:bg-carbon-light hover:text-navy transition-colors #{'bg-teal/10 text-teal' if request.path == employee_handbook_path}" do %>
                  <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4" />
                  </svg>
                  <div>
                    <div class="font-body font-normal">Employee Handbook</div>
                    <div class="text-xs text-carbon-heavy/60">Company policies & procedures</div>
                  </div>
                <% end %>

                <div class="flex items-center gap-3 px-4 py-3 rounded-lg text-gray-400 cursor-not-allowed">
                  <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10" />
                  </svg>
                  <div>
                    <div class="font-body font-normal">My Assets</div>
                    <div class="text-xs text-carbon-heavy/60">Coming soon</div>
                  </div>
                </div>

                <%= link_to holidays_path, class: "flex items-center gap-3 px-4 py-3 rounded-lg text-gray-700 hover:bg-carbon-light hover:text-navy transition-colors #{'bg-teal/10 text-teal' if request.path == holidays_path}" do %>
                  <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
                  </svg>
                  <div>
                    <div class="font-body font-normal">Holidays</div>
                    <div class="text-xs text-carbon-heavy/60">Company & national holidays</div>
                  </div>
                <% end %>

                <% if current_user.admin? %>
                  <%= link_to admin_root_path, class: "flex items-center gap-3 px-4 py-3 rounded-lg text-gray-700 hover:bg-carbon-light hover:text-navy transition-colors #{'bg-teal/10 text-teal' if request.path.start_with?(admin_root_path)}" do %>
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z" />
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
                    </svg>
                    <div>
                      <div class="font-body font-normal">Admin Panel</div>
                      <div class="text-xs text-carbon-heavy/60">Manage users and settings</div>
                    </div>
                  <% end %>
                <% end %>
              </nav>

              <!-- Footer -->
              <div class="border-t border-cloudy p-4">
                <%= link_to logout_path,
                    data: {
                      turbo_method: :delete,
                      turbo_confirm: "Are you sure you want to logout?"
                    },
                    class: "w-full bg-navy hover:bg-navy/90 text-white px-4 py-3 rounded-lg text-sm font-body font-normal transition-all flex items-center justify-center gap-2" do %>
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" />
                  </svg>
                  <span>Logout</span>
                <% end %>
              </div>
            </div>
          </div>
        </div>
      </nav>
    <% end %>

    <main>
      <% if flash.any? %>
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mb-4 space-y-2 pt-6">
          <% flash.each do |type, message| %>
            <div class="<%= flash_class(type) %> rounded-lg p-4 flex items-center justify-between font-body font-light">
              <div><%= message %></div>
              <button onclick="this.parentElement.remove()" class="ml-4 text-carbon-heavy/60 hover:text-carbon-heavy transition-colors text-xl leading-none">&times;</button>
            </div>
          <% end %>
        </div>
      <% end %>

      <%= yield %>
    </main>

    <script>
      function toggleMobileMenu() {
        const menu = document.getElementById('mobile-menu');
        const openIcon = document.querySelector('.mobile-menu-open');
        const closeIcon = document.querySelector('.mobile-menu-close');

        if (menu.classList.contains('hidden')) {
          // Show menu
          menu.classList.remove('hidden');
          openIcon.classList.add('hidden');
          closeIcon.classList.remove('hidden');
          document.body.style.overflow = 'hidden';
        } else {
          // Hide menu
          menu.classList.add('hidden');
          openIcon.classList.remove('hidden');
          closeIcon.classList.add('hidden');
          document.body.style.overflow = '';
        }
      }

      // Close menu when pressing Escape key
      document.addEventListener('keydown', function(event) {
        if (event.key === 'Escape') {
          const menu = document.getElementById('mobile-menu');
          if (!menu.classList.contains('hidden')) {
            toggleMobileMenu();
          }
        }
      });
    </script>
  </body>
</html>
